# Data cache

The original UCI Online Retail dataset is downloaded on first run.

```bash
python scripts/fetch_data.py
```

The cached file is:

```text
data/online_retail.csv
```

It is ignored by Git because the original dataset is large.

See `../DATASET.md` for source, DOI, and license information.
